Before executing any `$gm-*` skill, FIRST read the full GodotMaker Codex
runtime mapping at `.agents/references/runtime-mapping.md`. 

That mapping is the single Codex compatibility layer for all GodotMaker role
skills.

GodotMaker documentation may use Claude-first surface vocabulary such as
`.claude/skills`, `.claude/agents`, `.claude/templates`, `/gm-build`, Task,
TodoWrite, worker, reviewer, verifier, analyst, and worktree. In Codex,
interpret those terms through the Codex mapping and execute the Codex-native
equivalent. Do not rewrite the shared GodotMaker docs inline; the mapping
reference is the compatibility layer.
